# Test-only dependencies. Not installed in production.
-r requirements.txt

pytest>=8.0

# Starlette's TestClient needs an HTTP client, and which one depends on the
# Starlette version pip resolves: 1.x wants httpx2, earlier releases want httpx.
# requirements.txt does not pin Starlette, so install both and let TestClient
# pick — the alternative is a CI break whenever the resolved version moves.
httpx>=0.27
httpx2>=2.13

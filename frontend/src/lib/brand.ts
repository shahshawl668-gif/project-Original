/**
 * What the product is called, in one place.
 *
 * The name used to be typed into the shell, the sign-in screen, the signup
 * copy, the browser title and the Excel exports. Renaming it meant finding all
 * of them and missing one — which is how a product ends up called two things.
 */

/** The product name, as it appears to anyone using it. */
export const PRODUCT_NAME = "PayrollCheck";

/** The line under the mark. Short enough to sit beside it at phone width. */
export const PRODUCT_TAGLINE = "India · Audit grade";

/** Browser tab and search results. */
export const PRODUCT_TITLE = `${PRODUCT_NAME} — India statutory payroll validation`;

export const PRODUCT_DESCRIPTION =
  "Independent PF, ESIC, PT, LWF and income tax validation for Indian payroll. " +
  "It checks the register your payroll system produced — it never runs payroll.";
